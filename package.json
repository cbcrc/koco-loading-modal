{
  "name": "koco-loading-modal",
  "version": "1.3.0",
  "authors": [
    "fcharest <frederik.charest@radio-canada.ca>"
  ],
  "description": "Loading modal form using knockout and bootstrap modal",
  "main": "src/koco-loading.js",
  "keywords": [
    "modal",
    "koco"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^3.3.6",
    "jquery": "^2.2.4",
    "knockout": "^3.4.0",
    "koco": "^1.0.0",
    "koco-modaler": "^1.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
